/** 
 * Add ~react-tabs.js as props "tab"
 * 
 * @typedef {{
 *   label: String,
 *   icon: String | JSX,
 *   reqAuth: String,
 *   render: JSX,
 *   iconPos: "top" | "left" | "right" | "bottom",
 *   noTransform: Boolean | false,
 *   spacing: Number | 5,
 *   alignment: "center" | "left" | "right",
 *   width: Number | 200,
 *   height: Number | 20
 * }} tab
 * 
 * @typedef {[tab]} tabbizoSchema
 */

/**
 * @type {import("IZOArc/LabIZO/Tabbizo/__typedef").tabbizoSchema}
 */

 export default {};